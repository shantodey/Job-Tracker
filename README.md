1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
   ans=>getElementById দিয়ে শুধু একটা নির্দিষ্ট ID ধরে, getElementsByClassName দিয়ে একটা ক্লাসের সব এলিমেন্ট একসাথে ধরে, 
   আর querySelector বা querySelectorAll দিয়ে ID, Class বা Tag ধরে আনা যায়।

2. How do you create and insert a new element into the DOM?
   ans=> createElement দিয়ে নতুন ইলিমেন্ট বানানো যায় যেমন-id,class,p,button। 
   innerText বা classList.add নতুন ক্লাস অ্যাড করা অথবা ভিতরে text চেঞ্জ করা যায় ।

3. What is Event Bubbling? And how does it work?
   ইভেন্ট বাবলিং হচ্ছে কোন একটা কিছুকে ক্লিক করার পর তার পেরেন্ট এবং তার প্যারেন্ট ধরে কাজ করা।

4. What is Event Delegation in JavaScript? Why is it useful?
   Unsure
5. What is the difference between preventDefault() and stopPropagation() methods?
   ans => preventDefault() ব্যবহার কর ব্রাউজার যাতে অটোমেটিক রিলোড না হয় তা বন্ধ করা যায়।
   stopPropagation() বাবল যাতে উপরের দিকে না আসে তা আটকানো যায় ব্যবহার করে।
